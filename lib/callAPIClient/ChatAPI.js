import { GetRequest, PostRequest, DeleteRequest, PutRequest } from "../apiClient"

export const getChats = async () => {
    try {

    } catch (error) {
        console.log('Chat error:', error);
        throw error;
    }
}

export const getMessages = async (ChatId) => {
    try {

    } catch (error) {
        console.log('Chat error:', error);
        throw error;
    }
}

export const createGroupChat = async (groupName) => {
    try {

    } catch (error) {
        console.log('Chat error:', error);
        throw error;
    }
}

export const addMember = async (userId) => {
    try {

    } catch (error) {
        console.log('Chat error:', error);
        throw error;
    }
}

export const removeMember = async (userId) => {
    try {

    } catch (error) {
        console.log('Chat error:', error);
        throw error;
    }
}

export const createMessage = async (chatId, content, repliedMessageId, mediaIds) => {
    try {

    } catch (error) {
        console.log('Chat error:', error);
        throw error;
    }
}

export const deleteMessage = async (messageId) => {
    try {

    } catch (error) {
        console.log('Chat error', error);
        throw error;
    }
}

export const changeNameGroup = async () => {
    try {

    } catch (error) {
        console.log('Chat error:', error);
        throw error;
    }
}

export const likeMessages = async () => {
    try {

    } catch (error) {
        console.log('Chat error:', error);
        throw error;
    }
}